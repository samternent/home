<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style type="text/css" media="screen">
      * {
        box-sizing: border-box;
      }
      h1,
      h2,
      h3,
      h4,
      h5 {
        margin: 0;
        padding: 0;
      }
      :root {
        --scrollPos: 100%;
        --mouse-x: 0;
        --mouse-y: 0;
      }

      /* System Fonts as used by GitHub */
      body {
        font-family: Georgia, serif;
        font-size: calc(15px + 0.390625vw);
        margin: 0;
        min-height: 100vh;
        background-origin: content-box;
        text-align: justify;
        padding: 1em;
      }

      h1 {
        font-size: 2em;
      }
      h2 {
        font-size: 1.2em;
        font-weight: 100;
      }
      h1,
      h2 {
        letter-spacing: -0.1em;
      }

      h3 {
        position: sticky;
        top: 5.6em;
        background-color: #efeae7;
        padding: 0.5em 1em;
        margin: 1em 0;
        text-transform: uppercase;
        border-left: 0.1em solid #fd575a;
        /* border-left: 0.2em solid #9753ef; */
      }

      .content {
        max-width: 1080px;
        margin: 0 auto;
        padding: 0 1em;
      }
      .subcontent {
        padding: 0 1em;
      }

      a {
        color: #9753ef;
        transition: color;
      }
      a:hover {
        color: #fd575a;
      }

      .header {
        position: sticky;
        top: 0;
        padding: 2em 1em 1em;
        margin: 0 0 1em;
        z-index: 10;
      }
    </style>
    <title>api.footballsocial.app</title>
  </head>

  <body>
    <div class="content">
      <header class="header">
        <h1>API: FootballSocial</h1>
        <h2>The friendly Football score prediction game.</h2>
      </header>
      <h3>Welcome</h3>
      <p class="subcontent">
        This is the API for
        <a href="https://footballsocial.app">footballsocial.app</a>.
      </p>

      <h3>API</h3>
      <div class="subcontent">
        <div>
          <a href="/predict/PL/table"
            >GET: /predict/:competitionCode/table/:gameweek?</a
          >
          <p>Get the total or gameweek league table.</p>
        </div>
        <div>
          <a href="/predict/PL/calculate/7"
            >POST: /predict/:competitionCode/calculate/:gameweek</a
          >
          <p>Calculate the gameweek results</p>
        </div>
        <div>
          <a href="/predict/sam/PL/7"
            >GET: /predict/:username/:competitionCode/:gameweek</a
          >
          <p>Get gameweek user predictions</p>
        </div>
      </div>
    </div>

    <script type="module" src="js/main.js"></script>
  </body>
</html>
