# AI Rules - Routes

- Preserve folder-based route modules with `index.ts` files.
- Keep route component names CamelCase and prefixed with `Route`.
- Match folder structure to URL structure.
- Prefer lazy imports for route components.
