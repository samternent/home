# LLM Context - Routes

When editing routes:

- Update matching `index.ts` module for every new route component.
- Keep redirects explicit and deterministic.
- Do not introduce file-system auto-routing plugins.
- Verify URL mapping examples in tests.
