# LLM Context - Identity Module

Key constraints:

- Use `@ternent/identity` as source of crypto functions.
- Use `ternent-utils` for deterministic fingerprint hashing helpers.
- Keep provider API stable for route components.
