# CLAUDE.md

Runtime guidance:
- Runtime UIs are projection consumers, not security authorities.
- Surfaces should read via typed API selectors and write via typed API mutations only.
- Keep staging explicit (command now, commit/discard separately).
- Use `ternent-ui` components and keep route behavior consistent with `/w/...` contracts.
