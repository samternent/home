# AI Rules - Schema Layer

- Commands are explicit and typed.
- Schema packages define meaning above raw commits.
- In phase 1, schema definitions live inside plugin packages.
- Keep schema metadata separate from UI concerns.
- Compatibility decisions should be explainable from manifests, not hard-coded route logic.
- Hosted apps may depend on schemas, but schema definitions must not depend on hosted app UI.
