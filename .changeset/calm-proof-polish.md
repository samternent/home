---
"ternent-ui": minor
"ternent-utils": minor
---

Add shared UI primitives (`SDisclosure`, `SRadioGroup`, `STextarea`) and align proof app usage with `ternent-ui`.

Add `hashBytes` utility for deterministic raw-byte hashing in browser signing/verification flows.
