services:
  ternent-api:
    image: "samternent/ternent-api"
    restart: always
    ports:
      - "8001:3000"
    env_file:
      - '../.env'

  footballsocial-api:
    image: "samternent/footballsocial-api"
    restart: always
    ports:
      - "8002:3000"
    env_file:
      - '../.env'

  concords-api:
    image: "samternent/concords-api"
    restart: always
    ports:
      - "8003:3000"
    env_file:
      - '../.env'

  # redis:
  #   image: "redis:alpine"
  #   restart: always
  #   env_file:
  #     - '../.env'

  # db:
  #   image: postgres
  #   restart: always
  #   env_file:
  #     - '../.env'
  #   ports:
  #     - 6664:5432
