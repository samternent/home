apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-gov-kit
  namespace: backend
  annotations:
    cert-manager.io/issuer: letsencrypt-nginx
    acme.cert-manager.io/http01-edit-in-place: "true"
spec:
  tls:
    - hosts:
        - gov.ternent.dev
      secretName: letsencrypt-nginx-gov-kit
  rules:
    - host: gov.ternent.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gov-kit
                port:
                  number: 80
  ingressClassName: nginx
