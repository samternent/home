# Prompt — AI Agent (Execution Mode)

Implement the plan in `PLAN.md`.

Rules:
- Smallest changes possible that satisfy SPEC.
- Write tests alongside changes.
- Update docs if behaviour changes.
- Match existing repo conventions.

PR description must include:
- Checklist from `CHECKLIST.md`
- Endpoints added/changed
- Example token format + example receipt
