{
  "name": "@ternent/ledger",
  "version": "0.2.0",
  "description": "Append-only state engine for Concord",
  "license": "ISC",
  "files": [
    "dist",
    "SPEC.md"
  ],
  "type": "module",
  "sideEffects": false,
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./browser": {
      "types": "./dist/index.d.ts",
      "import": "./dist/browser.js",
      "default": "./dist/browser.js"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build:browser": "pnpm --filter @ternent/armour build && vite build -c vite.browser.config.ts",
    "build": "pnpm --filter @ternent/armour build && vite build && vite build -c vite.browser.config.ts",
    "test": "pnpm run build && vitest run"
  },
  "dependencies": {
    "@ternent/armour": "workspace:*",
    "@ternent/concord-protocol": "workspace:*",
    "@ternent/identity": "workspace:*",
    "@ternent/seal-cli": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.6.3",
    "vite": "^4.5.3",
    "vite-plugin-dts": "^1.7.3",
    "vitest": "^1.6.0"
  }
}
