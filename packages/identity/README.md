# Identity

`ternent-identity` is the legacy P-256 identity package currently used by Concord and PixPax.

New Seal work should use [`@ternent/identity`](../identity-v2/README.md), which provides the seed-based Ed25519 identity model and X25519 bridge helpers used by Seal v2.
